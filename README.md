# complex-task

React + TypeScript – for building a modular, type-safe interface.
Material UI (v5) – component-based UI library for structured and consistentdesign.
Emotion – CSS-in-JS styling solution to handle scoped and dynamic styles.
Zustand – lightweight state management for global and local data.
React Hook Form + Yup – for form handling and validation.
React Router v6 – for navigation between pages and protected routes.
Context API + MUI ThemeProvider – for global theme and shared state handling.
LocalStorage – to persist projects, tasks, theme, and session data.
